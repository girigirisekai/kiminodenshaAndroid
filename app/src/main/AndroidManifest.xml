<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.densha.kimi.kiminodensha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logodensha"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logodensha"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".JoinMemberActivity"/>
        <activity android:name=".FavActivity"/>
        <activity android:name=".SearchStationActivity"/>
        <activity android:name=".ElderlySeatsActivity"/>
        <activity android:name=".CarActivity"/>

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
</manifest>